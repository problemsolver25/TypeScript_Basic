let superHeros: string[] = [];
superHeros.push("Spiderman");
console.log(superHeros)

type User = {
    name: string,
    isActive: boolean
}
let allusers: User[] = [];
allusers.push({
    name: "John",
    isActive: true
});
console.log(allusers)

let MLModel: number[][] = [
    [255, 255, 255],
    []
];
console.log(MLModel)

export {}